# 当前审查摘要

2026-08-31。完整矩阵见[协议审查](docs/PROTOCOL_AUDIT.md)，测量见[验收报告](docs/ACCEPTANCE.md)。

## 本轮修复

本次已修改/编译/部署板端，详见[BOARD_FIXES](docs/BOARD_FIXES.md)。原优先发现中的FrameInfo/UDP内存边界、frames==0恢复限制、工厂文件覆写、setting坏请求/持久化、config_file、相机HTTP状态机已处理。主动断链进一步暴露eth0恢复后专用路由消失，已将专用路由/辅助地址修复纳入重连；最后验证结果见修复记录，不仅看一次“恢复”日志。

配置采用原子写入及fsync；真实设置4Hz后服务重启，独立订阅4.0005Hz，已写回5Hz。文件通道传真实配置与cmd18实测内参，解析和字节比对通过。没有伪造联合标定外参。

进一步修复cmd19目标UDP端口字节序错误：设备小端协议的7687为07 1E，不是1E 07；读回已确认192.168.0.179:7687。正常测量改为只读路由检查，不发额外cmd0；仅连续TCP接通而恢复测量失败才升级复位，不能把拔线期间连接失败算成硬件需复位。相机保持原生JPEG；1000帧多轮约9.89–9.99Hz，不能宣称严格10Hz已通过。

## 修复前发现（保留）

1. P1：`board/src/xt_radar.cpp:521` 按外部infosize拷贝到固定FrameInfo，边界不安全；UDP分片重复/总长也需加固。
2. P1：`:982` 只在frames==0重连，实际运行后断流不走恢复分支；最终巡检发现真实断流，需手动重启xt_radar服务恢复，整机热插拔不能判通过。
3. P1：`:111`、`:736` 每次启动截断重写Lixel.yaml，可能损坏其他工厂键。
4. P1：`:681`起setting只写内存，无持久化，坏PB仍返回成功；实际只读回复图像fps/format为0。
5. P1：config_file没有实现；没有真实文件源、FileMsgArray发送或触发约定。
6. P2：`board/src/xt_camera.cpp:243`起HTTP仅分段700ms等待，无总请求期限，Connection:close与SIGPIPE处理不足。最终短测仍有约1秒图像停顿。
7. P2：图像1000帧9.8239Hz、点云4.7257Hz，严格10/5Hz不能判通过；硬编码config/QoS/seq共用/日志和RTC问题详见矩阵。

## 已验证

- 相机100/1000帧顶层ImageMsgArray、array1、format2、1920×1080、完整JPEG、可解码、seq连续/stamp递增。
- 点云100/1000帧Array/嵌套坐标/scaler1000通过；PC保留全部收到的点，最终WS哈希比对0不一致。
- 网页和本机Foxglove可显示实际两路数据；JPEG不转码、不二次压缩。
- PC共9项单元通过；新增沉默源保留订阅测试。板端新增ASan/UBSan、2万组随机畸形输入及真实TCP HTTP服务器测试通过。PC恢复测试新增雷达沉默而图像继续、真实Zenoh peer关闭再开启的自动恢复验证。

## 暂缓/未做

30分钟测试遵照用户要求暂停；用户已实际给雷达及相机重新供电，原板端进程均自行恢复。相机一度未供电造成0帧，已由用户补电恢复；不可将该窗口记作网页/协议故障。真实电源恢复没有同步计时，精确恢复时长未验收。实际板卡/PC重启、多播多设备、完整5分钟记录轮转尚未完成本轮验收。本轮不修改IMU业务。未继续操作Foxglove界面或导出布局。

## 后续失败与修正

必须保留evidence/board-release-steady-20260831的7分钟失败：直接图像最大296ms，Foxglove图像最大2078ms，雷达最大31.437秒且结束时仍断流。PC已取消沉默时周期重建会话，增加各阶段耗时。90秒双流及独立恢复测试通过，但不能声称此前自然断流原因已经被唯一确定。实际控制通道事件现已记录；单元测试中的cmd157/status7是合成报文，不是传感器过热实测证据。

最终交换机版本97b843f5已经把UDP采集与Zenoh发布线程隔离，有界队列与过期/失败计数保留。12秒发送阻断中实际put阻塞2471ms，采集继续，没有雷达复位；相机直接/WS均无超过500ms间隔。雷达故障窗口69帧缺失，不能称无损；详见switch-publish-fault-20260831。此测试验证背压隔离和恢复，不证明所有历史自然故障都来自同一原因。

现场改线后的eth0没有carrier，eth1访问雷达3/3成功。已增加radar_network配置并保存eth1/.250，重启读回目标正确。旧eth0链路故障脚本在共享拓扑明确拒绝运行，不关闭会同时中断SSH/相机的eth1。板端配置/文件通道再次18项通过；当前源码及二进制哈希以board-switch-manifest-20260831.json为准。

最终210秒实际收包：JPEG2079帧9.895121Hz、雷达1050帧5.002429Hz，最大间隔238.178/323.751ms；直接和Foxglove均无500ms以上间隔，接收seq/解析/时间戳错误0，JPEG解码错误0。100/1000帧完整分位数见BOARD_FIXES；证据switch-final-1000-20260831。点云含0个有效点的完整帧，未假装每帧点数恒定。严格10Hz、长稳、完整联合标定等仍不宣称通过。

旧记录已归档，不能引用其中“全部正常”作当前结论。当前已改动板端源码，哈希及测试版本以[BOARD_FIXES](docs/BOARD_FIXES.md)为准。保留问题：setting.image_format协议两节冲突、联合外参缺失、QoS推荐值尚未显式全部适配、SN唯一性、RTC及板端服务/日志运维；不宣称全协议无条件通过。
